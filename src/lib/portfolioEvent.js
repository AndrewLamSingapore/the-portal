export function portalEvent(eventType,payload,options={}) { const evidenceLevel=options.evidenceLevel||'E0'; if(!String(eventType).startsWith('portal.')) throw new Error('portal event type required'); return {version:'1.0',event_id:'portal-'+Date.now()+'-'+Math.random().toString(16).slice(2),event_type:eventType,source:'the-portal',occurred_at:new Date().toISOString(),correlation_id:options.correlationId||null,subject_id:options.subjectId||null,evidence_level:evidenceLevel,provenance:Array.isArray(options.provenance)?options.provenance:[],payload:{...payload}}; }
