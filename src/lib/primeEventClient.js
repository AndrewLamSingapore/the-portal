export async function sendToPrime(event,{url=process.env.PRIME_EVENT_URL,token=process.env.PRIME_SPINE_TOKEN,fetchImpl=fetch}={}) { if(!url||!token) return {sent:false,reason:'PRIME event connection not configured'}; const response=await fetchImpl(url,{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${token}`},body:JSON.stringify(event)}); if(!response.ok) throw new Error(`PRIME event ingestion failed: ${response.status}`); return {sent:true,response:await response.json()}; }
