import {getArtifact,hasDatabase} from '../lib/db.js';export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=300');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Method not allowed'})}if(!hasDatabase())return res.status(503).json({error:'Archive unavailable'});const a=await getArtifact(String(req.query?.id||''));if(!a)return res.status(404).json({error:'Artifact not found'});return res.status(200).json({id:a.id,evidence_level:a.evidence_level||'AI-CURATED',sources:a.sources||[],notice:a.evidence_level==='HISTORICALLY-VERIFIED'?'Curator-reviewed source trail.':'Discovery object: not independently verified historical authority.'})}