export default function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=3600');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Method not allowed'})}return res.status(200).json({exhibitions:[{id:'late-arrivals',title:'Futures That Arrived Decades Late',question:'What had to change before these ideas could become ordinary?'},{id:'money-machines',title:'Machines That Tried to Replace Money',question:'How many ways can a society imagine trust before it calls one of them money?'}]})}