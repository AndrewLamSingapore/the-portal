import {findArtifacts,hasDatabase} from '../lib/db.js';export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Method not allowed'})}if(!hasDatabase())return res.status(503).json({ok:false});const a=await findArtifacts({limit:60}),verified=a.filter(x=>x.evidence_level==='HISTORICALLY-VERIFIED').length,sourced=a.filter(x=>(x.sources||[]).length).length;return res.status(200).json({ok:true,objects:a.length,verified,sourced,schema_versions:[...new Set(a.map(x=>x.schema_version))].sort(),oldest:a.length?Math.min(...a.map(x=>x.year)):null,newest:a.length?Math.max(...a.map(x=>x.year)):null})}