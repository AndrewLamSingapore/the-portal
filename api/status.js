import {hasDatabase,findArtifacts} from '../lib/db.js';export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');try{const database=hasDatabase(),a=database?await findArtifacts({limit:1}):[];return res.status(database&&a.length?200:503).json({status:database&&a.length?'OPERATIONAL':'DEGRADED',archive:database&&a.length>0,generation:Boolean(process.env.OPENAI_API_KEY),version:4,checked_at:new Date().toISOString()})}catch{return res.status(503).json({status:'DEGRADED',version:4,checked_at:new Date().toISOString()})}}