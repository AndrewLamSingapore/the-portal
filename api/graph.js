import {findArtifacts,hasDatabase} from '../lib/db.js';export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=300');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'Method not allowed'})}if(!hasDatabase())return res.status(503).json({error:'Archive unavailable'});const items=(await findArtifacts({limit:60})).sort((a,b)=>a.year-b.year),nodes=items.map(a=>({id:a.id,year:a.year,title:a.title,evidence_level:a.evidence_level,concepts:a.concepts})),edges=[];for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const c=(nodes[i].concepts||[]).filter(x=>(nodes[j].concepts||[]).includes(x));if(c.length)edges.push({from:nodes[i].id,to:nodes[j].id,type:'CONCEPTUAL_ECHO',concepts:c})}return res.status(200).json({nodes,edges:edges.slice(0,100)})}