import {hasDatabase,findArtifacts} from '../lib/db.js';export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');try{const db=hasDatabase(),items=db?await findArtifacts({limit:60}):[],checks={database:db,generation:Boolean(process.env.OPENAI_API_KEY),archive:items.length>0,evidence_schema:items.length===0||items.every(a=>'evidence_level'in a&&'sources'in a&&'relationships'in a)};return res.status(Object.values(checks).every(Boolean)?200:503).json({ok:Object.values(checks).every(Boolean),version:4,checks})}catch{return res.status(503).json({ok:false,version:4})}}