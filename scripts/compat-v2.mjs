import assert from 'node:assert/strict';import fs from 'node:fs';const db=fs.readFileSync('lib/db.js','utf8'),schema=fs.readFileSync('schema.sql','utf8');assert.ok(schema.includes("default 'AI-CURATED'"));assert.ok(schema.includes("default '[]'::jsonb"));assert.ok(db.includes("a.evidence_level||'AI-CURATED'"));assert.ok(db.includes('a.sources||[]'));assert.ok(db.includes('a.relationships||[]'));console.log('PASS: legacy artifacts remain compatible with V2 evidence defaults.');