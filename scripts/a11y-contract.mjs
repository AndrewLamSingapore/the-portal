import assert from 'node:assert/strict';import fs from 'node:fs';const h=fs.readFileSync('index.html','utf8');for(const x of ['<html lang="en">','aria-live="polite"','focus-visible','prefers-reduced-motion'])assert.ok(h.includes(x),`missing accessibility contract: ${x}`);assert.ok(h.includes('button'), 'living graph must expose keyboard-focusable controls');console.log('PASS: living graph accessibility baseline contract.');