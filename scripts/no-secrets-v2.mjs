import assert from 'node:assert/strict';import fs from 'node:fs';for(const f of ['index.html','v2.html','V2.md','CHANGELOG.md']){const s=fs.readFileSync(f,'utf8');assert.ok(!/sk-[A-Za-z0-9_-]{20,}/.test(s),`${f} contains key-like material`);assert.ok(!/postgres(?:ql)?:\/\/[^\s]+:[^\s]+@/i.test(s),`${f} contains database credential-like material`)}console.log('PASS: no secret-like material in public V2 assets.');