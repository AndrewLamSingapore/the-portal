import assert from 'node:assert/strict';import fs from 'node:fs';const h=fs.readFileSync('index.html','utf8');for(const phrase of ['Some futures arrive. Some fail. Some wait for the world to become capable of them.','AI-CURATED · DISCOVERY','HISTORICALLY VERIFIED · SOURCED','CONCEPTUAL INFERENCE · NOT CAUSATION','OPEN AN UNEXPECTED DRAWER'])assert.ok(h.includes(phrase),`missing first-screen contract: ${phrase}`);console.log('PASS: first-30-seconds experience contract.');