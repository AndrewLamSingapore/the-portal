const $=x=>document.getElementById(x);let data={artifacts:[]},current=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function badge(a){const e=a.evidence_level||'AI-CURATED';return `<span class="badge ${e==='HISTORICALLY-VERIFIED'?'verified':e==='CONCEPTUAL-INFERENCE'?'inference':'ai'}">${esc(e)}</span>`}function show(a){if(!a)return;current=a;$('detail').innerHTML=`${badge(a)}<h2>${esc(a.title)}</h2><p>${esc(a.description)}</p><div class="k">${a.year} · ${esc(a.status)}</div><p><b>The future it imagined</b><br>${esc(a.imagined_future)}</p><p><b>What became of it</b><br>${esc(a.modern_descendant)}</p><div class="rels">${(a.relationships||[]).map(r=>`<span class="rel">${esc(r.type)} · ${esc(r.label)}</span>`).join('')}</div><div class="sources">${(a.sources||[]).length?'<h3>Source trail</h3>'+a.sources.map(s=>`<a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">${esc(s.title)}</a>`).join(''):'<p><b>Source trail:</b> not independently verified. Treat this object as a discovery prompt, not historical authority.</p>'}</div>`;$('drawer').classList.add('open')}function render(){const x=data.artifacts||[];$('grid').innerHTML=x.map(a=>`<button class="item" data-id="${esc(a.id)}">${badge(a)}<b>${esc(a.title)}</b><small>${a.year} · ${esc(a.status)}<br>${esc((a.concepts||[]).slice(0,3).join(' · '))}</small></button>`).join('');$('track').innerHTML=[...x].sort((a,b)=>a.year-b.year).map(a=>`<button class="item year" data-id="${esc(a.id)}"><strong>${a.year}</strong><span>${esc(a.title)}</span></button>`).join('');$('exhibitions').innerHTML=(data.exhibitions||[]).map(e=>`<article class="exhibit"><h3>${esc(e.title)}</h3><p>${esc(e.subtitle)}</p><small>${e.artifacts.length} objects in this lens</small></article>`).join('');document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>show(x.find(a=>a.id===b.dataset.id)))}async function load(){const r=await fetch('/api/archive?limit=60',{cache:'no-store'});if(!r.ok)throw Error();data=await r.json();render()}$('open').onclick=()=>{if(data.artifacts.length)show(data.artifacts[Math.floor(Math.random()*data.artifacts.length)])};$('jump').onclick=async()=>{const r=await fetch('/api/serendipity'+(current?'?from='+encodeURIComponent(current.id):''),{cache:'no-store'});if(r.ok)show(await r.json())};$('close').onclick=()=>$('drawer').classList.remove('open');load().catch(()=>{$('grid').innerHTML='<p>The stacks are temporarily unavailable.</p>'});
